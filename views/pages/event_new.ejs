<!DOCTYPE html>
<html lang="en">
<head>
    <title>Event Manager</title>
    <% include ../partials/header.ejs %>
</head>
<body>
    <div class="container">
        <div class="center-block">
            <h2>Create Event</h2>
            <% if (typeof errors != 'undefined') { %>
                <div class="alert alert-danger">
                    <% errors.forEach(function(error) { %>
                        <div><%= error %></div>
                    <% }) %>
                </div>
            <% } %>
            <form action="/events/new" method="POST">
                <div class="form-group">
                    <div><label for="name">Name</label></div>
                    <div><input id="name" name="name" type="text" value="<%= locals.name || '' %>" class="form-control" /></div>
                </div>
                <div class="form-group">
                    <div><label for="description">Description</label></div>
                    <div><textarea id="description" name="description" class="form-control"><%= locals.description || '' %></textarea></div>
                </div>
                <div class="form-group">
                    <div><label for="start">Start Date and Time</label></div>
                    <div><input id="start" name="start" type="text" value="<%= locals.start || ''%>" class="form-control" /></div>
                </div>
                <div class="form-group">
                    <div>
                        <input type="submit" value="Create" class="btn btn-default" />
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>
</html>